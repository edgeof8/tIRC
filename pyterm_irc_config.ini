[Connection]
default_server = irc.quakenet.org
default_port = 6667
default_ssl = false
default_nick = pyRCDev
default_channels = #python
password =
nickserv_password =
auto_reconnect = true
verify_ssl_cert = false

[UI]
message_history_lines = 500
colorscheme = default
# Future: Add specific color settings like:
# color_join_part_fg = green
# color_status_bar_bg = blue

[Logging]
log_enabled = true
# log_dir is now handled by the application, defaults to a "logs" subdirectory
log_file = pyrc.log
log_level = INFO # DEBUG, INFO, WARNING, ERROR, CRITICAL
log_max_bytes = 15242880 # 5 MB in bytes (1024 * 1024 * 5)
log_backup_count = 3

[General]
ignore_list = BadUser1,SpammerNick
# Future: Aliases
# alias_g = /msg GlobalUser Hello!
